{
  "name": "asset-studio-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Asset Studio: a three.js tool to author models, textures and landscape for the wowd game client",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "typescript": "^5.9.0",
    "vite": "^7.0.0"
  }
}
