# Матрица трассируемости

> Файл генерируется командой `make trace` из `specs/*/spec.md`, `features/**/*.feature` и
> `web/tests/**`. Правьте спеки, сценарии и тесты, а не этот файл.

Критериев приёмки: **21**. Без покрытия: **0**.

| Критерий | Спека | Требование | Сценарии |
|---|---|---|---|
| REQ-000-1 | 000 Closed | Ассет каждого вида (`character`, `creature`, `item`, `landscape`, `texture`) сохраняется с именем, … | `features/assets/saving.feature` — An asset of every kind can be saved<br>`features/assets/saving.feature` — A freshly saved asset was created and changed at the same moment<br>`web/tests/unit/asset.spec.ts` — spec |
| REQ-000-2 | 000 Closed | Список отдаёт метаданные всех ассетов, новые сверху; пустая студия — пустой список | `features/assets/browsing.feature` — The list shows every saved asset, newest first<br>`features/assets/browsing.feature` — An empty studio lists nothing |
| REQ-000-3 | 000 Closed | Сохранённый ассет открывается по id; нагрузка отдаётся байт-в-байт с Content-Type своего формата (`… | `features/assets/browsing.feature` — A saved asset can be opened again by id<br>`features/assets/browsing.feature` — The payload comes back byte for byte with the content type of its format |
| REQ-000-4 | 000 Closed | Повторное сохранение под тем же id обновляет ассет на месте: id и время создания сохраняются, время… | `features/assets/saving.feature` — Saving again under the same id replaces the payload and keeps the id<br>`features/assets/saving.feature` — A metadata-only save keeps the payload |
| REQ-000-5 | 000 Closed | Удаление убирает ассет и его нагрузку: его нет в списке, по id — «не найдено», файлов на диске нет;… | `features/assets/browsing.feature` — Deleting removes the asset and its payload<br>`features/assets/browsing.feature` — Deleting what does not exist is not found |
| REQ-000-6 | 000 Closed | Сохранение отклоняется (400, причина в ответе) при пустом имени, неизвестном виде, пустом или неизв… | `features/assets/saving.feature` — A save with a missing or unknown field is refused and writes nothing<br>`features/assets/saving.feature` — A format that is a path cannot write outside the data directory<br>`features/assets/saving.feature` — A new asset without a payload is refused<br>`features/assets/saving.feature` — An id chosen by the client must be a safe file name |
| REQ-000-7 | 000 Closed | Формат меняется только вместе с новой нагрузкой; при смене формата прежний файл нагрузки удаляется … | `features/assets/saving.feature` — The format cannot change without a new payload<br>`features/assets/saving.feature` — Changing the format with a new payload leaves no orphan file |
| REQ-000-8 | 000 Closed | Манифест версии 1 содержит ровно те ассеты, у которых есть `wowdRef`; запись несёт `wowdRef`, `kind… | `features/bridge/manifest.feature` — Only assets bound to a wowd id are in the manifest, and each entry is loadable<br>`features/bridge/manifest.feature` — A studio with nothing bound hands the game an empty list<br>`features/bridge/manifest.feature` — Unbinding an asset takes it out of the manifest |
| REQ-000-9 | 000 Closed | В dev-режиме API отвечает на preflight и разрешает вызовы с другого origin (методы GET/POST/PUT/DEL… | `features/ops/dev-mode.feature` — The frontend on another origin may call the API<br>`features/ops/dev-mode.feature` — Every answer carries the allowed origin in development mode<br>`features/ops/dev-mode.feature` — With CORS disabled no origin is allowed<br>`features/ops/dev-mode.feature` — Health answers ok |
| REQ-000-10 | 000 Closed | Неизвестный id, а также id, не являющийся безопасным именем файла (разделители пути, точки), — «не … | `features/assets/browsing.feature` — An id that names nothing, or is not a safe file name, is not found |
| REQ-000-11 | 000 Closed | «New placeholder» показывает во вьюпорте заготовку своего вида в метрах: капсула для тела, коробка … | `features/e2e/studio-ui.feature` — A new placeholder of every kind appears in the viewport at metre scale<br>`web/tests/e2e/studio-ui.spec.ts` — spec<br>`web/tests/e2e/studio-ui.spec.ts` — @req-000-11 A new ${p.kind} placeholder appears in the viewport at metre scale<br>`web/tests/unit/session.spec.ts` — spec |
| REQ-000-12 | 000 Closed | «Save» экспортирует объект вьюпорта в GLB (текстуру — в PNG), ассет появляется в списке, клик по не… | `features/e2e/studio-ui.feature` — Save exports the placeholder and the asset comes back from the list<br>`features/e2e/studio-ui.feature` — A texture is saved as a PNG<br>`web/tests/e2e/studio-ui.spec.ts` — @req-000-12 Save exports the placeholder and the asset comes back from the list<br>`web/tests/e2e/studio-ui.spec.ts` — @req-000-12 A texture is saved as a PNG<br>`web/tests/unit/asset.spec.ts` — spec<br>`web/tests/unit/session.spec.ts` — spec |
| REQ-000-13 | 000 Closed | С флагом `-web` studiod отдаёт собранный фронтенд и API с одного origin; неизвестный путь отдаёт `i… | `features/e2e/studio-ui.feature` — In production the app and the API come from one origin<br>`web/tests/e2e/studio-ui.spec.ts` — @req-000-13 In production the app and the API come from one origin<br>`web/tests/unit/api.spec.ts` — spec |
| REQ-001-1 | 001 Closed | Импорт `.glb` или самодостаточного `.gltf` показывает модель во вьюпорте и кадрирует камеру на неё;… | `features/editor/import.feature` — A GLB from disk appears in the viewport, framed and measured<br>`features/editor/import.feature` — A self-contained glTF imports the same way<br>`features/editor/import.feature` — A name the designer already typed is kept<br>`web/tests/e2e/import.spec.ts` — @req-001-1 A GLB from disk appears in the viewport, framed and measured<br>`web/tests/e2e/import.spec.ts` — @req-001-1 A self-contained glTF imports the same way<br>`web/tests/e2e/import.spec.ts` — @req-001-1 A name the designer already typed is kept<br>`web/tests/unit/asset.spec.ts` — spec<br>`web/tests/unit/import.spec.ts` — spec<br>`web/tests/unit/session.spec.ts` — spec |
| REQ-001-2 | 001 Closed | Импорт отклоняется с сообщением в строке статуса и не меняет вьюпорт, если: файл не glTF (ни GLB-за… | `features/editor/import.feature` — A file the studio cannot use is refused with the reason<br>`web/tests/e2e/import.spec.ts` — @req-001-2 A file the studio cannot use is refused with the reason: ${row.file} as ${row.kind}<br>`web/tests/unit/import.spec.ts` — spec<br>`web/tests/unit/session.spec.ts` — spec |
| REQ-001-3 | 001 Closed | Модель целиком переносится, поворачивается и масштабируется гизмо (режимы W/E/R) и числовыми полями… | `features/editor/transform.feature` — Typing into the transform fields moves the model<br>`features/editor/transform.feature` — Dragging the gizmo updates the fields<br>`features/editor/transform.feature` — The gizmo modes follow W, E and R<br>`features/editor/transform.feature` — A field accepts only a usable number<br>`web/tests/e2e/transform.spec.ts` — @req-001-3 Typing into the transform fields moves the model<br>`web/tests/e2e/transform.spec.ts` — @req-001-3 Dragging the gizmo updates the fields<br>`web/tests/e2e/transform.spec.ts` — @req-001-3 The gizmo modes follow W, E and R<br>`web/tests/e2e/transform.spec.ts` — @req-001-3 A field accepts only a usable number:<br>`web/tests/unit/commands.spec.ts` — spec<br>`web/tests/unit/model.spec.ts` — spec<br>`web/tests/unit/session.spec.ts` — spec |
| REQ-001-4 | 001 Closed | Undo/redo (кнопки и Ctrl+Z / Ctrl+Shift+Z) для трансформаций и правок материала; глубина не меньше … | `features/editor/history.feature` — A move is undone and redone from the keyboard<br>`features/editor/history.feature` — A material edit is undone with the button<br>`features/editor/history.feature` — A new edit after undo forgets the redo<br>`features/editor/history.feature` — Fifty steps can be undone<br>`features/editor/history.feature` — One gizmo drag is one step<br>`features/editor/history.feature` — Saving does not forget the history<br>`web/tests/e2e/history.spec.ts` — @req-001-4 A move is undone and redone from the keyboard<br>`web/tests/e2e/history.spec.ts` — @req-001-4 A material edit is undone with the button<br>`web/tests/e2e/history.spec.ts` — @req-001-4 A new edit after undo forgets the redo<br>`web/tests/e2e/history.spec.ts` — @req-001-4 Fifty steps can be undone<br>`web/tests/e2e/history.spec.ts` — @req-001-4 One gizmo drag is one step<br>`web/tests/e2e/history.spec.ts` — @req-001-4 Saving does not forget the history<br>`web/tests/unit/commands.spec.ts` — spec<br>`web/tests/unit/session.spec.ts` — spec |
| REQ-001-5 | 001 Closed | Клик по мешу выбирает его (подсветка во вьюпорте, имя меша в панели); панель материала показывает и… | `features/editor/material.feature` — Clicking a mesh selects it and shows its material<br>`features/editor/material.feature` — Editing the material changes the selected mesh only<br>`features/editor/material.feature` — A shared material changes on every mesh that uses it<br>`features/editor/material.feature` — Clicking empty space clears the selection<br>`web/tests/e2e/material.spec.ts` — @req-001-5 Clicking a mesh selects it and shows its material<br>`web/tests/e2e/material.spec.ts` — @req-001-5 Editing the material changes the selected mesh only<br>`web/tests/e2e/material.spec.ts` — @req-001-5 A shared material changes on every mesh that uses it<br>`web/tests/e2e/material.spec.ts` — @req-001-5 Clicking empty space clears the selection<br>`web/tests/unit/commands.spec.ts` — spec<br>`web/tests/unit/model.spec.ts` — spec<br>`web/tests/unit/session.spec.ts` — spec |
| REQ-001-6 | 001 Closed | Save после правок экспортирует GLB под тем же id, и после повторного открытия совпадают: число меше… | `features/editor/resave.feature` — What was saved is what comes back<br>`features/editor/resave.feature` — Saving again keeps the id<br>`web/tests/e2e/resave.spec.ts` — @req-001-6 What was saved is what comes back<br>`web/tests/e2e/resave.spec.ts` — @req-001-6 Saving again keeps the id |
| REQ-001-7 | 001 Closed | После любой правки или импорта появляется маркер «изменено» (в статусе и заголовке вкладки); New pl… | `features/editor/unsaved.feature` — An import marks the asset as modified<br>`features/editor/unsaved.feature` — Saving clears the mark<br>`features/editor/unsaved.feature` — Leaving unsaved work asks first, and no means stay<br>`features/editor/unsaved.feature` — Confirming discards the work<br>`web/tests/e2e/unsaved.spec.ts` — @req-001-7 An import marks the asset as modified<br>`web/tests/e2e/unsaved.spec.ts` — @req-001-7 Saving clears the mark<br>`web/tests/e2e/unsaved.spec.ts` — @req-001-7 Leaving unsaved work asks first, and no means stay: ${action.title}<br>`web/tests/e2e/unsaved.spec.ts` — @req-001-7 Confirming discards the work<br>`web/tests/unit/session.spec.ts` — spec |
| REQ-001-8 | 001 Closed | Сервер отклоняет сохранение с нагрузкой больше лимита кодом 413 и сообщением, называющим лимит в ме… | `features/assets/limits.feature` — A payload over the limit is refused and the limit is named<br>`features/assets/limits.feature` — A payload exactly at the limit is accepted<br>`web/tests/unit/import.spec.ts` — spec |
